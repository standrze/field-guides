{"text": "A backup restoration exercise checks whether stored backups can actually restore a service. Record the recovery steps and verify the restored data."}
{"text": "Authentication failures can have several causes, including expired credentials and user error. A failure count alone does not establish an intrusion."}
